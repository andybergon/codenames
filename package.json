{
  "name": "codenames",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "node --check scripts/generate-clue-index.mjs && node --check scripts/smoke.mjs && node --check src/word-data.js && node --check src/clue-index.js && node --check src/embeddings.js && node --check src/model.js && node --check src/app.js && node scripts/smoke.mjs && vite build",
    "generate:words": "python3 scripts/generate-clue-words.py",
    "generate:index": "node scripts/generate-clue-index.mjs",
    "generate:data": "npm run generate:words && npm run generate:index"
  },
  "dependencies": {
    "@huggingface/transformers": "3.8.1",
    "lucide": "^1.23.0"
  },
  "devDependencies": {
    "vite": "8.1.4"
  }
}
